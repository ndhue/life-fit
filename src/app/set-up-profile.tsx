import { Text, View } from "react-native";
import { global } from "../constants/Global";

const SetUpProfile = () => {
  return (
    <View style={global.wrapper}>
      <Text>Tổng quan về bạn</Text>
    </View>
  )
}

export default SetUpProfile;